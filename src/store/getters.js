const themeCls = state => state.themeCls
const viewWidth = state => state.viewWidth
const viewHeight = state => state.viewHeight
const viewZoom = state => state.viewZoom
export {themeCls, viewWidth, viewHeight, viewZoom}
